# Fiji v2 Phase 1C handoff

**Date:** 27 July 2026
**Current package:** v2.0.3
**Active MUIO case/run:** `Fiji_v2` / `Phase1C_BottomUp`

## Current state

Phase 1C sector electricity accounting and the selected bottom-up projection
are implemented in the live source case. The run is Optimal with objective
`-1573.67149091`. The historical accounting control is exactly objective- and
dispatch-identical to Phase 1B.

The result-free portable case is:

```text
muio/Fiji_v2_v2.0.3_MUIO.zip
SHA-256 da1b1e078bb223636a33c50ba55b5cf63ec28d2b34705e4c67c4b252e1588c9d
```

It contains editable MUIO source JSON and view metadata. It excludes `res/`,
generated solver data, LP and results.

## Trace the active demand

Start with:

1. `data_sources/MODEL_DATA_MAP.csv`, row `M-1C-02`;
2. `data_sources/DATA_SOURCES.md`;
3. `data_sources/ASSUMPTIONS.csv`, IDs beginning `A-1C-`;
4. `data_sources/CALCULATIONS.csv`, IDs beginning `C-1C-`;
5. `data_sources/evidence/energy/PHASE_1C_PROJECTION_SOURCE_EXTRACTS_2026-07-27.md`;
6. `data_sources/calculation_notes/PHASE_1C_BOTTOM_UP_ELECTRICITY.md`; and
7. `data_sources/evidence/energy/fiji_phase1c_bottom_up_electricity_projection_2020_2050.csv`.

The build manifest pins the active model, evidence, generated-result and
portable-archive hashes:
`diagnostics/calibration_runs/build_manifest.json`.

## Validation

- Dedicated Phase 1C validation: 15/15 passed.
- General Fiji technical validation: 15/15 passed.
- Data-lineage validation: 8/8 passed.
- Reserve proxy: `CURRENT`, zero mismatches.
- Result identity and freshness: passed.

Reports are under `diagnostics/calibration_runs/phase1c/`.

## Resume

Import or unpack the v2.0.3 archive in MUIO, regenerate the solver input and
solve through the normal application chain. Do not copy generated solver
files from another laptop.

The next model-development gate is to parameterize the documented LEDS
scenario variants and ±10% demand-driver sensitivity before activating any
useful-service branch. Residential cooking remains quantity-blocked.
