# Six-ledger migration

This is a provenance-only change. It creates the canonical SOURCES, CALCULATIONS, ASSUMPTIONS, MODEL_MAP, GAPS and CHANGES tables, plus an inventory of every active Fiji v2.6 JSON file. No model parameter JSON or result was changed. SHA-256 fields are intentionally blank at the user's direction.
